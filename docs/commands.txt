# GitHub
git clone https://github.com/rtphokie/hello-world

# Python (optional, Mac already has 2.7 installed)
https://www.python.org/downloads/

git clone https://github.com/rtphokie/hello-world.git
cd hello-world
pip install –r requirements
python application.py

open http://localhost:5000


# install Jenkins
sudo yum update –y¤
sudo yum install -y git
sudo wget -O /etc/yum.repos.d/jenkins.repo http://pkg.jenkins-ci.org/redhat/jenkins.repo¤sudo rpm --import https://pkg.jenkins.io/redhat/jenkins.io.key
sudo yum –y install java-1.8.0 
sudo yum –y remove java-1.7.0-openjdk 
sudo yum install –y jenkins
sudo service jenkins start



